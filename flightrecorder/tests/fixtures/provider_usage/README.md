# Provider usage fixtures

Small JSON examples for provider usage accounting tests.

- `normal.json` - one typical paid call
- `breach.json` - one call that crosses hard-stop
- `missing_pricing.json` - model not in pricing table
- `mismatch.json` - provider/model mismatch

All values are fake. No real provider keys, tokens, or costs.
